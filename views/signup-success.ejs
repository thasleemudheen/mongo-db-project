<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Success</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }
        h1 {
            text-align: center;
            margin-top: 50px;
        }
        div.details {
            width: 400px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
        }
        p {
            margin-bottom: 10px;
            font-size: 18px;
        }
        .button-container {
            text-align: center;
        }
        .button-container button {
            margin: 5px;
            padding: 8px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
        }
        .button-container button.edit a {
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            font-size: 14px;
            margin: 5px;
            padding: 8px 15px;
            border-radius: 5px;

        }
        .button-container button.delete  {
            background-color: #dc3545;
            color: #fff;
            
        }
        .button-container button.logout {
            background-color: #28a745;
            color: #fff;
        }
        .buttonform{
            display: flex;
            justify-content: center;
        }
    </style>
</head>
<body>
    <h1>Welcome <%= user.name %></h1>
    <div class="details">
        <p><strong>Name:</strong> <%= user.name %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Phone:</strong> <%= user.phone %></p>
      <div class="buttonform">
        <form action="/edit" method="post" class="button-container">
            <button class="edit" id="editbtn"><a href="/edit/<%= user._id %>">Edit</a></button>
        </form>
        <form id="deleteForm" action="/delete" method="post" class="button-container">
            <input type="hidden" name="userId" value="<%= user._id %>"> 
            <button class="delete">Delete</button>
        </form>
        <form action="/logout" method="get" class="button-container">
            <button class="logout">Logout</button>
        </form>
    </div>
    </div>

    <script>
        document.getElementById("deleteForm").addEventListener("submit", function(event) {
            event.preventDefault(); 
            if (confirm("Are you sure you want to delete this user?")) {
                
                this.submit();
            } else {
                
            }
        });
    </script>
</body>
</html>